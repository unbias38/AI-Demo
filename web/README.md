# 簡易網頁部署說明

這個資料夾包含一個簡單的 `index.html`。要在本地端查看網頁，請依照以下步驟操作：

1. 安裝 Python (版本 3.8 以上)。
2. 在終端機進入此資料夾，執行下列指令啟動簡易 HTTP 伺服器：
   
   ```bash
   python3 -m http.server 8000
   ```
3. 開啟瀏覽器，造訪 <http://localhost:8000> 即可看到網頁內容。

若要部署到 GitHub Pages 或其他主機，也可以直接將 `index.html` 上傳至對應位置。
